# Backend Environment Variables
NODE_ENV=development
PORT=4001

# MongoDB connection strings (using local MongoDB via Compass)
# Original database for existing functionality
MONGODB_URI=mongodb://localhost:27017/ai_ingest

# Vector Search Database (new database for vector search features)
VECTOR_SEARCH_DB_URI=mongodb://localhost:27017/vector_search_db

# Supabase config
# JWKS URL used for local verification (preferred)
SUPABASE_JWKS_URL=https://<project-ref>.supabase.co/auth/v1/keys
# Optional: fallback verification via Supabase REST if JWKS not set
SUPABASE_URL=https://<project-ref>.supabase.co
SUPABASE_ANON_KEY=

# Comma-separated list of allowed origins
CORS_ORIGIN=http://localhost:5173

# OpenAI API Key (REQUIRED for vector search features)
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=

# Admin email allowlist (comma separated). First one is the primary admin.
ADMIN_EMAIL=lokeshpawar721@gmail.com
